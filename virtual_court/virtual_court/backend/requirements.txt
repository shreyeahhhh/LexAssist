fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
pydantic==2.5.0
python-dotenv==1.0.0
groq==0.12.0
# multipart parsing (required for evidence upload via FastAPI UploadFile)
python-multipart==0.0.9
# evidence extraction (PDF / image OCR / DOCX). Image OCR (pytesseract) also
# requires the system Tesseract binary; if missing, the Groq vision path still
# works on its own.
PyMuPDF>=1.23.0
Pillow>=10.0.0
pytesseract>=0.3.10
python-docx>=1.1.0

